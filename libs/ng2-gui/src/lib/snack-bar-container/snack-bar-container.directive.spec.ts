/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SnackBarContainerDirective } from './snack-bar-container.directive';

describe('Directive: SnackBarContainer', () => {
  it('should create an instance', () => {
    const directive = new SnackBarContainerDirective();
    expect(directive).toBeTruthy();
  });
});
